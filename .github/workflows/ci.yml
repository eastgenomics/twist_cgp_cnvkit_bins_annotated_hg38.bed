name: ci
on: [push, pull_request]
jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Python syntax check
        run: python3 -m py_compile build_twist_cgp_cnvkit_bins.py derive_annotation_normalization_table.py
      - name: Unit tests
        run: pip install "pytest==8.3.4" && pytest test_build_twist_cgp_cnvkit_bins.py -v
